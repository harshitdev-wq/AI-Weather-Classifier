[project]
name = "ai-weather-classifier"
version = "1.0.0"
description = "ResNet18 weather image classifier with FastAPI inference."
requires-python = "~=3.12.0"

[tool.vercel]
runtime = "python3.12"
